---
$id: schema:availableChannel
$type: rdf:Property
label: schema:availableChannel
rdfs:comment: "A means of accessing the service (e.g. a phone bank, a web site, a location, etc.)."
rdfs:label: "availableChannel"
schema:domainIncludes: {"@id":"schema:Service"}
schema:rangeIncludes: {"@id":"schema:ServiceChannel"}
---

# schema:availableChannel

