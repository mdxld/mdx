---
$id: https://schema.org/businessFunction
$type: http://www.w3.org/1999/02/22-rdf-syntax-ns#Property
label: businessFunction
comment: |
  The business function (e.g. sell, lease, repair, dispose) of the offer or component of a bundle (TypeAndQuantityNode). The default is http://purl.org/goodrelations/v1#Sell.
rdfs:comment: "The business function (e.g. sell, lease, repair, dispose) of the offer or component of a bundle (TypeAndQuantityNode). The default is http://purl.org/goodrelations/v1#Sell."
rdfs:label: "businessFunction"
schema:contributor: "https://schema.org/docs/collab/GoodRelationsTerms"
schema:domainIncludes: ["https://schema.org/Demand","https://schema.org/TypeAndQuantityNode","https://schema.org/Offer"]
schema:rangeIncludes: "https://schema.org/BusinessFunction"
---

# businessFunction

The business function (e.g. sell, lease, repair, dispose) of the offer or component of a bundle (TypeAndQuantityNode). The default is http://purl.org/goodrelations/v1#Sell.

