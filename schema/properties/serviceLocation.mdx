---
$id: https://schema.org/serviceLocation
$type: http://www.w3.org/1999/02/22-rdf-syntax-ns#Property
label: serviceLocation
comment: |
  The location (e.g. civic structure, local business, etc.) where a person can go to access the service.
rdfs:comment: "The location (e.g. civic structure, local business, etc.) where a person can go to access the service."
rdfs:label: "serviceLocation"
schema:domainIncludes: "https://schema.org/ServiceChannel"
schema:rangeIncludes: "https://schema.org/Place"
---

# serviceLocation

The location (e.g. civic structure, local business, etc.) where a person can go to access the service.

