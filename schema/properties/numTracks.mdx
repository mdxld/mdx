---
$id: schema:numTracks
$type: rdf:Property
label: schema:numTracks
rdfs:comment: "The number of tracks in this album or playlist."
rdfs:label: "numTracks"
schema:domainIncludes: {"@id":"schema:MusicPlaylist"}
schema:rangeIncludes: {"@id":"schema:Integer"}
---

# schema:numTracks

